# HTTPS origin so phones can use getUserMedia (iOS blocks camera on plain HTTP LAN URLs).
# Copy to `.env.local`. IP: `ipconfig getifaddr en0`
VITE_PUBLIC_ORIGIN=https://192.168.0.42:5173

# Optional — only if you do not use the Vite WSS proxy:
# VITE_SIGNAL_URL=wss://192.168.0.42:8787
